{# templates/products/product.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Products List{% endblock %}

{% block body %}
<div class="flex items-center justify-center min-h-screen">
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4"> 🧰 Products List</h1>
           <ul class="grid grid-cols-2 gap-4">
            {% for product in products %}
                <li class="book-card bg-white shadow-md rounded-lg overflow-hidden">
                    <a href="{{ path('product_details', {ulid: product.ulid}) }}" class="block p-4 hover:bg-gray-100 transition duration-300">
                        <h2 class="text-lg font-semibold">{{ product.name }} ▸</h2>
                        <p class="text-sm text-gray-600 mt-1">{{ product.description }}</p>
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
      </div>
{% endblock %}




